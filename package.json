{
  "name": "devknobs",
  "version": "0.0.1",
  "private": false,
  "license": "MIT",
  "author": "Mert Duzgun",
  "description": "drop-in dev panel for emulating browser preferences, locale, geolocation and layout knobs from inside the page",
  "type": "module",
  "types": "./dist/index.d.ts",
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./global": "./dist/index.global.js"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --format esm && bun build src/global.ts --outfile dist/index.global.js --format iife --minify && tsc --emitDeclarationOnly --project tsconfig.build.json",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.1",
    "typescript": "^5.9.3"
  }
}
